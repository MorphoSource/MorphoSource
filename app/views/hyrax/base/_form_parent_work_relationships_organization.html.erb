<div id="parent-relationships-organizations" class="form-group mt-0" data-behavior="parent-relationships-organizations" data-param-key="<%= f.object.model_name.param_key %>" data-members="<%= f.object.member_of_works_json('Organization') %>" data-new-work-created="" data-repeatable="no">
  <div class="form-inline">
    <div><label><%= f.label "Organization" %></label></div>
    <%= f.input_field :find_parent_work,
                      prompt: :translate,
                      autocomplete: 'off',
                      data: {
                        autocomplete: 'work',
                        'autocomplete-url' => find_organization_autocomplete_url()
                      } %>
    <a class="btn btn-sm btn-primary" id="btn-add-organization" data-behavior="add-relationship">Add organization</a>
  </div>

  <div class="message has-warning hidden"></div>
  <table class="table table-striped">
    <div style="margin-top: 10px">
  </div>
    <tbody>
    </tbody>
  </table>
</div>

<script type="text/x-tmpl" id="tmpl-parent-work-organizations">
<tr>
  <td>{%= o.title %}</td>
  <td><button class="btn btn-sm btn-danger" data-behavior="remove-relationship" data-confirm-text="<%= t('.confirm.text') %>" data-confirm-cancel="<%= t('.confirm.cancel') %>" data-confirm-remove="<%= t('.confirm.remove') %>"><%= t('.actions.remove') %></button></td>
</tr>
</script>
