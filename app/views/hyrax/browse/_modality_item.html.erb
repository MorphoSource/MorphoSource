<h2>
	<%= link_to modality, main_app.media_search_path("f[media_modality_ssim][]" => modality) %><br/>
  <span class="counts">
  	<%= "#{media_count_by_modality(modality)} Media".pluralize(media_count_by_modality(modality)) %>
	</span>
</h2>
