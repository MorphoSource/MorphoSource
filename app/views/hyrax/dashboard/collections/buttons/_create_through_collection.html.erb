<%= link_to t('hyrax.dashboard.collections.actions.create_new_media_in_' + presenter.collection_type_title + '.label'), main_app.new_submission_path(collection: presenter.id), class: 'btn btn-primary' %>
