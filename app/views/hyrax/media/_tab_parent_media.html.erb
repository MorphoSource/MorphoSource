<%
if @presenter.parent_media.count == 0
  added_class = 'one-circle-only'
else
  added_class = ''
end
%>
<div class="col-6 indicators-wrapper">
  <h2>PARENT MEDIA HIERARCHY</h2>
  <% if @presenter.parent_media.present? %>
    <%= render partial: 'edit_parent_media_list', collection: @presenter.parent_media, as: :member %>
  <% else %>
    <div class="p-0">
      <div class="indicators line solid table-cell">
        <p><span class="circle first"></span></p>
      </div>
      <div class="table-cell media-title">
        <p>Raw undeposited parent media</p>
      </div>
    </div>
  <% end %>

  <div class="p-0">
    <div class="indicators line solid one-circle-only table-cell">
      <p><span class="circle"></span></p>
    </div>
    <div class="table-cell media-title">
      <p><%= @presenter.link_name_long %> (this media)</p>
    </div>
  </div>

</div>

<div class="col-6">
  <div class="content-block">
    <h2>SELECT OR CHANGE PARENT MEDIA</h2>
    <%= render 'form_parent_work_relationships_media', p: p, has_processing_event: 'true' %>
  </div>
</div>
