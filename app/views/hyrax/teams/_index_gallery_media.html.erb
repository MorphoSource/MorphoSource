<% 
id = document.id 
bso, bso_media_count, cho, cho_media_count = physical_object_from_media(id)
%>
<% doc_presenter = index_presenter(document) %>
<%# default partial to display solr document fields in catalog index view -%>

<dl class="document-metadata dl-horizontal dl-invert">
  <dd><%= document.id %></dd>
  <% if @bso_member_count > 0 %>
    <dd class="break-all"><%= bso&.title&.first %></dd>
    <dd><%= bso&.taxonomies&.first&.title&.first %></dd>
  <% end %>
  <% if @cho_member_count > 0 %>
    <dd class="break-all"><%= cho&.title&.first %></dd>
  <% end %>
  <dd class='long-badge'><%= render_publication_status_badge(document) %></dd>
</dl>
