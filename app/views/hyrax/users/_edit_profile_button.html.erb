<% if @current_user.admin? %>
  <div id='edit-profile-button' style='margin-top:15px;'>
    <%= button_to 'Edit profile', main_app.profile_edit_path(@user.ms_id), method: :get, class: "btn btn-primary" %>
  </div>
<% end %>
