<% if current_user&.admin? %>
  <div class="admin-csv-links mb-1">
    <div class="collection-export-links">
      <b>Admin functions:</b>
    </div>
    <div class="collection-export-links">
      <%= link_to request.parameters.merge({:format => :csv, :per_page => 1000000}) do %>
        <i class="fas fa-file-csv fa-lg"></i> <span>Save current media or objects query with facets applied</span>
      <% end %>
    </div>
    <div class="collection-export-links">
      <% if @presenter.team? && @presenter.organization.present? %>
        <%= link_to main_app.team_media_projects_path(id: @presenter.id, :format => :csv, :per_page => 1000000, 'f[intersections][]' => 'organization_not_team' ) do %>
          <i class="fas fa-file-csv fa-lg"></i> <span>Save projects with media not owned by team</span>
        <% end %>
      <% end %>
    </div>
    <div class="collection-export-links">
      <%= link_to media_downloads_csv_url(@collection) do %>
        <i class="fas fa-file-csv fa-lg"></i> <span>Save all media downloads</span>
      <% end %>
    </div>
    <div class="collection-export-links">
      <%= link_to media_requests_csv_url(@collection) do %>
        <i class="fas fa-file-csv fa-lg"></i> <span>Save all media download requests</span>
      <% end %>
    </div>
  </div><!-- /admin-csv-links -->
<% end %>
