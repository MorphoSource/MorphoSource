<a id="add-selected-to-cart" class="btn btn-sm btn-default disabled" href="javascript:void(0)">Add <i class="material-icons">check_box</i> to download</a>
<% if current_ability.can? :edit, @collection%>
  <% if params["f"].present? %>
    <a id="save-media-order-disabled" class="btn btn-sm btn-default" disabled="disabled" style="background-color: #f5f5f5;
  color: black;">Remove Filter to Save Order</a>
  <% else %>
    <a id="save-media-order" class="btn btn-sm btn-default">Save Order</a>
  <% end %>
<% end %>