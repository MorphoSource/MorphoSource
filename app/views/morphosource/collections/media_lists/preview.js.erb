$('#preview-wrapper').html(
  "<%= j (render partial: 'preview') %>"
)
$('.name-container').html(
  "<%= j (render partial: 'preview_name') %>"
)

var mediaList = document.querySelector("div.media-list-results")?.mediaList;
mediaList?.toggleViewerDisabledWrap();
