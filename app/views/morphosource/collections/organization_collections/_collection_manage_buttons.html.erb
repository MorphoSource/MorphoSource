<div class="row collection-manage-buttons pull-right">
  <% if @can_edit %>
    <div class="collection-manage-button-wrapper">
      <%= link_to "Edit", hyrax.edit_dashboard_collection_path(@presenter), title: "t('hyrax.collection.actions.edit.desc')", class: 'btn btn-primary' %>
    </div>
    <div class="collection-manage-button-wrapper">
      <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Export <span class="caret"></span>
      </button>
      <ul class="dropdown-menu">
        <li>
          <%= link_to t("morphosource.collections.organization.exports.#{@tab.to_s}.media_export.label"), media_export_csv_url(@collection), class: 'btn btn-primary' %>
        </li>
        <li>
          <%= link_to t("morphosource.collections.organization.exports.#{@tab.to_s}.media_download_counts.label"),
          media_download_counts_csv_url(@collection), class: 'btn btn-primary'
          %>
        </li>
        <li>
          <%= link_to t("morphosource.collections.organization.exports.#{@tab.to_s}.media_downloads.label"),
            media_downloads_csv_url(@collection), class: 'btn btn-primary'
          %>
        </li>
        <li>
          <%= link_to t("morphosource.collections.organization.exports.#{@tab.to_s}.media_requests.label"), media_requests_csv_url(@collection),
            class: 'btn btn-primary'
          %>
        </li>
      </ul>
    </div>
  <% end %>
</div>
