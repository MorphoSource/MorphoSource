<%= simple_form_for @collection,
    url: main_app.dashboard_collection_unlink_organization_path,
    method: 'post',
    html: { id: 'remove-organization-form' } do |f| %>
  <%= submit_tag 'Remove organization', class: 'btn btn-sm btn-danger', style: 'position:relative; left:25px; font-size:14px;', data: { confirm: 'Are you sure you want to remove the organization from this team?' } %>
<% end %>
