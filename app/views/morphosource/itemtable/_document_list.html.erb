<div class="table-responsive" id="documents">
  <table class="table table-striped works-list itemtable teams-projects">
  <thead>
    <tr>
      <%= render 'list_headers' %>
    </tr>
  </thead>
  <tbody>
    <% @items.each_with_index do |item, counter| %>
      <%= render 'list_item', item: item, counter: counter %>
    <% end %>
  </tbody>
</table>
</div>
