<div class="collection-count">
  <div class="col-12"><span class="badge badge-info d-inline-block"><%= membership.capitalize %></span>
    <span><%= count %> <%= count > 1 ? "Organizations" : "Organization" %></span></div>
</div>

