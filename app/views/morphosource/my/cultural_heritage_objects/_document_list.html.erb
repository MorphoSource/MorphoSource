<table id="datatable-cho-list" class="table table-striped teams-projects">
  <caption class="sr-only">List of items in this team</caption>
  <thead>
  <tr>
    <th><%= sortable_table_header('Identifier', 'title_ssi') %></th>
    <th><%= sortable_table_header('Material', 'material_si') %></th>
    <th><%= sortable_table_header('CHO Type', 'cho_type_si') %></th>
    <th><%= sortable_table_header('Vouchered', 'vouchered_si') %></th>
    <th>Total Media</th>
    <th><%= sortable_table_header('Date Added', 'date_uploaded_dtsi') %></th>
  </tr>
  </thead>
  <tbody>
    <%= render partial: 'document_list_row', collection: @document_list, as: :document %>
  </tbody>
</table>
