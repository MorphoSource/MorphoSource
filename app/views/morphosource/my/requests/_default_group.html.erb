<table class="table table-striped works-list">
  <caption class="sr-only"><%= t("hyrax.dashboard.my.sr.listing") %> <%= application_name %></caption>
  <thead>
  <tr>
    <th class="check-all"><label for="check_all" class="sr-only"><%= t("hyrax.dashboard.my.sr.check_all_label") %></label><%= render_check_all %></th>
    <th><%= t("hyrax.dashboard.my.heading.title") %></th>
    <th>ID</th>
    <th>Object</th>
    <th>Taxonomy</th>
    <th>Status</th>
    <th>Owner</th>
    <th>Affiliation</th>
    <th>Requested</th>
    <th>Approved</th>
    <th>Denied</th>
    <th>Canceled</th>
    <th>Expired</th>
    <th>Actions</th>
  </tr>
  </thead>
  <tbody>
    <% @items.each_with_index do |item, counter| %>
      <% document = solr_doc_find(item.work_id) %>
      <% if document.present? %>
        <%= render 'morphosource/my/requests/list_works', document: document, counter: counter, item: item, presenter: Hyrax::MediaPresenter.new(document, current_ability) %>
      <% end %>
    <% end %>
  </tbody>
</table>
