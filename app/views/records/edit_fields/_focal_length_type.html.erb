<% focal_length_types_service = Morphosource::FocalLengthTypesService.new %>
<%= f.input :focal_length_type, as: :select,
	collection: focal_length_types_service.select_all_options(f.object.focal_length_type),
    include_blank: true,
    input_html: { class: 'form-control' },
    wrapper_html: { class: 'ie_photogrammetry' }
%>
