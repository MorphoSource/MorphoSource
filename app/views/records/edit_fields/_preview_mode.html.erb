<%= f.input :preview_mode, as: :select,
    collection: ["Interactive/Embeddable", "Thumbnail Only"],
    label: 'Media preview mode',
    include_blank: local_assigns[:blank_label],
    input_html: { class: 'form-control' }
%>
