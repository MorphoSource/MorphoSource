<% sex_field_service = Morphosource::SexFieldService.new %>
<%= f.input :sex, as: :select,
	collection: sex_field_service.select_all_options(f.object.sex),
    include_blank: true,
    input_html: { class: 'form-control select optional form-control' } %>
