<div class="hyc-blacklight hyc-bl-pager">
  <div class="row record-padding paginate-wrapper ms-works-pagination-wrapper">
    <% if @response.docs.count == 0 %>
      <div class="col-xs-12">
        <div class="row">
          <div class="no-result">
            No records found.
          </div>
        </div>
      </div>
    <% else %>
      <div class="col-md-12 col-lg-2">
        <%= render 'shared/lists/rows_per_page' %>
      </div>
      <div class="col-md-12 col-lg-2">
        <div>Showing <%= page_entries_info @response %></div>
      </div>
      <div class="col-md-12 col-lg-8">
        <% if @response.total_pages > 1 %>
          <%= paginate @response, outer_window: 2, theme: 'blacklight', route_set: main_app %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
