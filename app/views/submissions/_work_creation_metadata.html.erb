<%= render partial: 'work_creation_metadata_terms', locals: { f: f, terms: f.object.primary_terms, term_group: 'base-terms' } %>

<% if f.object.display_additional_fields? %>
  <%= render partial: 'work_creation_metadata_terms', locals: { f: f, terms: f.object.secondary_terms, term_group: 'additional-terms' } %>
<% end %>
