development: <%= "#{ ENV["REDIS_HOST"] || "localhost" }:6379" %> 
test: <%= "#{ ENV["REDIS_HOST"] || "localhost" }:6379" %> 
production: <%= ENV['REDIS_URL'] || "redis://:#{ ENV["REDIS_PASSWORD"] || "" }@#{ ENV["REDIS_HOST"] || "localhost" }:6379/" %>