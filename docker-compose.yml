services:
  app: &app
    build:
      context: .
      target: morphosource-dev
      args:
        - EXTRA_APK_PACKAGES=git less
    container_name: app
    stdin_open: true
    tty: true
    user: "101292838:0"
    env_file:
      - ./vendor/docker/credentials.env
      - ./vendor/docker/docker-compose.env
    depends_on:
      - db_migrate
      - fcrepo
      - memcached
      - postgres
      - redis
      - solr
    ports:
      - 3000:3000
    volumes:
      - .:/app/samvera/hyrax-webapp
      - app-vendor-bundle:/app/samvera/hyrax-webapp/vendor/bundle
      - app-bundle:/app/samvera/hyrax-webapp/.bundle/
      - app-node-modules:/app/samvera/hyrax-webapp/node_modules
      - hyrax-derivatives:/app/samvera/hyrax-webapp/derivatives
      - hyrax-uploads:/app/samvera/hyrax-webapp/uploads
      - rails-tmp:/app/samvera/hyrax-webapp/tmp
    networks:
      - morphosource

  app_worker:
    build:
      context: .
      target: morphosource-worker-dev
    container_name: app_worker
    user: "101292838:0"
    env_file:
      - ./vendor/docker/credentials.env
      - ./vendor/docker/docker-compose.env
    depends_on:
      - db_migrate
      - fcrepo
      - memcached
      - postgres
      - redis
      - solr
    volumes:
      - .:/app/samvera/hyrax-webapp
      - app-vendor-bundle:/app/samvera/hyrax-webapp/vendor/bundle
      - app-bundle:/app/samvera/hyrax-webapp/.bundle/
      - app-node-modules:/app/samvera/hyrax-webapp/node_modules
      - hyrax-derivatives:/app/samvera/hyrax-webapp/derivatives
      - hyrax-uploads:/app/samvera/hyrax-webapp/uploads
      - rails-tmp:/app/samvera/hyrax-webapp/tmp
    networks:
      - morphosource

  app_test:
    build:
      context: .
      target: morphosource-worker-dev
    command: tail -f /dev/null # do nothing but wait for rspec commands
    container_name: app_test
    user: "101292838:0"
    env_file:
      - ./vendor/docker/credentials.env
      - ./vendor/docker/docker-compose.env
      - ./vendor/docker/docker-compose-test.env
    depends_on:
      - db_migrate
      - fcrepo
      - memcached
      - postgres
      - redis
      - solr
    profiles:
      - test
    volumes:
      - .:/app/samvera/hyrax-webapp
      - app-vendor-bundle:/app/samvera/hyrax-webapp/vendor/bundle
      - app-bundle:/app/samvera/hyrax-webapp/.bundle/
      - app-node-modules:/app/samvera/hyrax-webapp/node_modules
      - test-derivatives:/app/samvera/hyrax-webapp/derivatives
      - test-uploads:/app/samvera/hyrax-webapp/uploads
      - test-public:/app/samvera/hyrax-webapp/public
      - test-tmp:/app/samvera/hyrax-webapp/tmp
    networks:
      - morphosource

  db_migrate:
    build:
      context: .
      target: morphosource-dev
      args:
        - EXTRA_APK_PACKAGES=git less
    container_name: db_migrate
    user: "101292838:0"
    env_file:
      - ./vendor/docker/credentials.env
      - ./vendor/docker/docker-compose.env
    command: sh -c 'bin/uv-install && bundle && db-migrate-seed.sh'
    depends_on:
      - postgres
      - fcrepo
      - solr
    volumes:
      - .:/app/samvera/hyrax-webapp
      - app-vendor-bundle:/app/samvera/hyrax-webapp/vendor/bundle
      - app-bundle:/app/samvera/hyrax-webapp/.bundle/
      - app-node-modules:/app/samvera/hyrax-webapp/node_modules
      - rails-tmp:/app/samvera/hyrax-webapp/tmp
    networks:
      - morphosource

  db_migrate_test:
    build:
      context: .
      target: morphosource-worker-dev
      args:
        - EXTRA_APK_PACKAGES=git less
    container_name: db_migrate_test
    user: "101292838:0"
    env_file:
      - ./vendor/docker/credentials.env
      - ./vendor/docker/docker-compose.env
      - ./vendor/docker/docker-compose-test.env
    command: sh -c 'bundle && db-migrate-test.sh'
    depends_on:
      - postgres
      - fcrepo
      - solr
      - app_test
    profiles:
      - test
    volumes:
      - .:/app/samvera/hyrax-webapp
      - app-vendor-bundle:/app/samvera/hyrax-webapp/vendor/bundle
      - app-bundle:/app/samvera/hyrax-webapp/.bundle/
      - app-node-modules:/app/samvera/hyrax-webapp/node_modules
      - test-tmp:/app/samvera/hyrax-webapp/tmp
    networks:
      - morphosource

  lightbox:
    image: gitlab-registry.oit.duke.edu/morphosource/lightbox:0.0.2
    container_name: lightbox
    # ports:
    #   - 4000:3000
    networks:
      - morphosource

  postgres:
    image: postgres:16.9
    container_name: postgres
    env_file:
      - ./vendor/docker/credentials.env
      - ./vendor/docker/docker-compose.env
    # ports:
    #  - "5432:5432"
    volumes:
      - db:/var/lib/postgresql/data
    networks:
      - morphosource

  fcrepo:
    image: ghcr.io/samvera/fcrepo4:4.7.5
    container_name: fcrepo
    volumes:
      - fcrepo:/data
      - ./vendor/docker/fcrepo/fedora.xml:/var/lib/jetty/webapps/fedora.xml
    ports:
     - 8080:8080
    networks:
      - morphosource

  memcached:
    image: memcached
    container_name: memcached
    # ports:
    #  - '11211:11211'
    networks:
      - morphosource

  redis:
    image: redis:5-alpine
    container_name: redis
    volumes:
      - redis:/data
    networks:
      - morphosource

  solr:
    image: bitnamilegacy/solr:8.11.2-debian-11-r233
    container_name: solr
    environment:
      SOLR_CORES: "hyrax, hyrax_test"
      SOLR_CORE_CONF_DIR: /core_config/conf
    ports:
      - 8983:8983
    volumes:
      - ./solr/conf:/core_config/conf
      - solr_data:/bitnami
    networks:
      - morphosource

  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    container_name: minio
    command: server /data --console-address ":9001"
    env_file:
      - ./vendor/docker/credentials.env
      - ./vendor/docker/docker-compose.env
    ports:
      - 9000:9000
      - 9001:9001
    volumes:
      - minio_data:/data
    networks:
      - morphosource
    profiles:
      - object_storage

  minio_setup:
    image: minio/mc:RELEASE.2025-08-13T08-35-41Z
    container_name: minio_setup
    depends_on:
      - minio
    env_file:
      - ./vendor/docker/credentials.env
      - ./vendor/docker/docker-compose.env
    entrypoint: >
      /bin/sh -c "
      until mc alias set local http://minio:9000 \"$$MINIO_ROOT_USER\" \"$$MINIO_ROOT_PASSWORD\" >/dev/null 2>&1; do
        echo 'waiting for minio...'; sleep 1;
      done &&
      mc mb --ignore-existing \"local/$$S3_BUCKET\" &&
      echo 'Bucket ready.'
      "
    networks:
      - morphosource
    profiles:
      - object_storage

volumes:
  app-bundle:
  app-node-modules:
  app-vendor-bundle:
  db:
  fcrepo:
  hyrax-derivatives:
  hyrax-uploads:
  minio_data:
  rails-tmp:
  redis:
  solr_data:
  test-derivatives:
  test-uploads:
  test-public:
  test-tmp:

networks:
  morphosource:
    driver: bridge
    driver_opts:
      com.docker.network.bridge.name: br-morphosource
    name: morphosource
